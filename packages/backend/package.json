{
  "name": "backend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npx tsx --env-file=./../../.env --env-file=./.env src/index.ts",
    "dev": "npx tsx --env-file=./../../.env --env-file=./.env --watch src/index.ts"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "express": "^5.1.0",
    "express-session": "^1.18.2",
    "memory-level": "^3.1.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/express": "^5.0.3",
    "@types/express-session": "^1.18.2",
    "@types/node": "^24.7.0"
  }
}
