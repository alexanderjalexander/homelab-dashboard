{
  "name": "homelab-fastify",
  "module": "index.ts",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --workspaces build",
    "start": "bun run --workspaces start",
    "dev": "bun run --workspaces dev",
    "clean": "rm -rf node_modules backend/node_modules bun.lockb backend/bun.lockb"
  }
}